<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.wjc.bookmallbackend.mapper.OrderMapper">

    <resultMap id="BaseResultMap" type="top.wjc.bookmallbackend.entity.Order">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="userId" column="user_id"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="addressId" column="address_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="payTime" column="pay_time"/>
        <result property="shipTime" column="ship_time"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, order_no, user_id, total_amount, status, address_id, create_time, update_time, pay_time, ship_time
    </sql>

    <select id="countPendingShipmentByAddressId" resultType="int">
        SELECT COUNT(1)
        FROM `order`
        WHERE address_id = #{addressId}
          AND status = 2
    </select>

    <insert id="insert" parameterType="top.wjc.bookmallbackend.entity.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `order` (order_no, user_id, total_amount, status, address_id)
        VALUES (#{orderNo}, #{userId}, #{totalAmount}, #{status}, #{addressId})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `order`
        WHERE id = #{id}
    </select>

    <select id="selectByIdAndUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `order`
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM `order`
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(1)
        FROM `order`
        WHERE user_id = #{userId}
    </select>

    <update id="updateStatus">
        UPDATE `order` SET status = #{status} WHERE id = #{id}
    </update>

    <update id="updatePayTime">
        UPDATE `order` SET pay_time = #{payTime} WHERE id = #{id}
    </update>

    <update id="updateShipTime">
        UPDATE `order` SET ship_time = #{shipTime} WHERE id = #{id}
    </update>

</mapper>
